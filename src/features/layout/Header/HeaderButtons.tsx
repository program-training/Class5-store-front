import { Typography } from "@mui/material";

const HeaderButtons = () => {
  return (
    <Typography
      sx={{
        display: "flex",
        alignItems: "center",
        justifyContent: "space-between",
      }}
    ></Typography>
  );
};

export default HeaderButtons;
